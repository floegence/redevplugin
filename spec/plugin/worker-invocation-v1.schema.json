{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.redevplugin.dev/plugin/worker-invocation-v1.schema.json",
  "title": "ReDevPlugin worker invocation payload v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "plugin_id",
    "plugin_instance_id",
    "active_fingerprint",
    "worker_id",
    "worker_mode",
    "worker_scope",
    "artifact",
    "abi",
    "method",
    "export",
    "effect",
    "execution",
    "params"
  ],
  "properties": {
    "plugin_id": { "type": "string", "minLength": 1 },
    "plugin_instance_id": { "type": "string", "minLength": 1 },
    "active_fingerprint": { "type": "string", "minLength": 1 },
    "worker_id": { "type": "string", "minLength": 1 },
    "worker_mode": { "enum": ["job", "actor"] },
    "worker_scope": { "type": "string", "minLength": 1 },
    "artifact": {
      "type": "string",
      "minLength": 1,
      "pattern": "^workers/[A-Za-z0-9._/-]+\\.wasm$"
    },
    "abi": { "const": "redeven-wasm-worker-v1" },
    "method": { "type": "string", "minLength": 1 },
    "export": {
      "enum": ["redeven_worker_invoke", "redeven_actor_start", "redeven_actor_stop"]
    },
    "effect": { "enum": ["read", "write", "execute", "delete", "admin"] },
    "execution": { "enum": ["sync", "operation", "subscription"] },
    "surface_instance_id": { "type": "string" },
    "session_channel_id_hash": { "type": "string" },
    "bridge_channel_id": { "type": "string" },
    "params": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
