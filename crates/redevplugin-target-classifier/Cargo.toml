[package]
name = "redevplugin-target-classifier"
version = "0.0.0-dev"
edition = "2024"
license = "MIT"

[dependencies]

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
